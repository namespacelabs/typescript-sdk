// @generated by protoc-gen-connect-es v0.12.0 with parameter "target=js+ts+dts"
// @generated from file proto/namespace/cloud/iam/v1beta/tokens.proto (package namespace.cloud.iam.v1beta, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { BatchRevokeRevokableTokenRequest, BatchRevokeRevokableTokenResponse, CreateRevokableTokenRequest, CreateRevokableTokenResponse, IssueIdTokenRequest, IssueIdTokenResponse, IssueTenantTokenRequest, IssueTenantTokenResponse, ListRevokableTokensRequest, ListRevokableTokensResponse, RevokeRevokableTokenRequest } from "./tokens_pb.js";
import { Empty, MethodKind } from "@bufbuild/protobuf";

/**
 * TokenService provides facilities to obtain "tokens" to authenticate access to
 * Namespace. Tokens emitted by TokenService are always Tenant-scoped.
 *
 * It allows, for example,  exchanging tenant tokens for OIDC tokens to identify
 * Namespace workloads with external audiences.
 *
 * You can also create revokable tokens that can be used as more traditional API
 * keys.
 *
 * ## Access
 *
 * IAM services are available over gRPC and HTTP on the following endpoint:
 *
 * ```
 * https://iam.namespaceapis.com:443
 * ```
 *
 * See [the overview](https://buf.build/namespace/cloud) for examples accessing
 * the API from the console.
 *
 * ## Authentication
 *
 * Calls to the Token service require providing a JWT as a Bearer token, that
 * has been signed by the Namespace platform.
 *
 * Tenant tokens issued by Tenant service can be used as such Bearer tokens. The
 * token can be used as to authenticate calls to Token API with Authorization
 * header:
 *
 * ```
 * Authorization: Bearer {token}
 * ```
 *
 * @generated from service namespace.cloud.iam.v1beta.TokenService
 */
export declare const TokenService: {
  readonly typeName: "namespace.cloud.iam.v1beta.TokenService",
  readonly methods: {
    /**
     * Exchange a Namespace tenant token for an OIDC token for identity federation.
     * The generated token identifies Namespace tenants with the provided target audience.
     *
     * ### Authentication
     * Requires a tenant token with the {"federation", "issue_federation"} action permission.
     *
     * ### Errors
     * - If no audience is provided, a `InvalidArgument` status is returned.
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.IssueIdToken
     */
    readonly issueIdToken: {
      readonly name: "IssueIdToken",
      readonly I: typeof IssueIdTokenRequest,
      readonly O: typeof IssueIdTokenResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Produces credentials in the form of a Bearer token that grant access to
     * the specified Tenant. The credentials provided to the `IssueTenantToken`
     * call are validated to verify they are capable of requesting credentials.
     *
     * ### Authentication
     * Requires a "partner token".
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.IssueTenantToken
     */
    readonly issueTenantToken: {
      readonly name: "IssueTenantToken",
      readonly I: typeof IssueTenantTokenRequest,
      readonly O: typeof IssueTenantTokenResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Creates a revokable token that returns a bearer token (prefixed with "nsrt_") and metadata (with token_id prefixed with "tok_").
     * Revokable tokens can be explicitly revoked and have a maximum expiration of 90 days.
     *
     * ### Authentication
     * Requires a tenant token with the {"token/revokable", "create"} action permission.
     *
     * ### Errors
     * - If expiry is not provided or exceeds 90 days, an `InvalidArgument` status is returned.
     * - If no policies are defined, an `InvalidArgument` status is returned.
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.CreateRevokableToken
     */
    readonly createRevokableToken: {
      readonly name: "CreateRevokableToken",
      readonly I: typeof CreateRevokableTokenRequest,
      readonly O: typeof CreateRevokableTokenResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Revokes a revokable token by its token_id (prefixed with "tok_"), preventing the associated bearer token from being used for authentication.
     * Records who revoked the token and when.
     *
     * ### Authentication
     * Requires a tenant token with the {"token/revokable", "revoke"} action permission.
     *
     * ### Errors
     * - If the token_id is not found or already revoked, a `NotFound` status is returned.
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.RevokeRevokableToken
     */
    readonly revokeRevokableToken: {
      readonly name: "RevokeRevokableToken",
      readonly I: typeof RevokeRevokableTokenRequest,
      readonly O: typeof Empty,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Lists all revokable tokens for the authenticated tenant.
     *
     * ### Authentication
     * Requires a tenant token with the {"token/revokable", "list"} action permission.
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.ListRevokableTokens
     */
    readonly listRevokableTokens: {
      readonly name: "ListRevokableTokens",
      readonly I: typeof ListRevokableTokensRequest,
      readonly O: typeof ListRevokableTokensResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Batch revokes multiple revokable tokens based on filter criteria.
     * At least one filter must be provided.
     *
     * ### Authentication
     * Requires a tenant token with the {"token/revokable", "revoke"} action permission.
     *
     * ### Errors
     * - If no filters are provided, an `InvalidArgument` status is returned.
     *
     * @generated from rpc namespace.cloud.iam.v1beta.TokenService.BatchRevokeRevokableToken
     */
    readonly batchRevokeRevokableToken: {
      readonly name: "BatchRevokeRevokableToken",
      readonly I: typeof BatchRevokeRevokableTokenRequest,
      readonly O: typeof BatchRevokeRevokableTokenResponse,
      readonly kind: MethodKind.Unary,
    },
  }
};

